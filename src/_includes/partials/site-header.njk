{# =============================================================================
  partials/site-header.njk
  ------------------------------------------------------------------------------
  PURPOSE
  - Sticky top bar with brand + hamburger menu.
  - Hamburger uses a CSS-only checkbox toggle for simplicity and reliability.
  - On mobile, bottom-nav is the primary navigation.
  - On tablet/desktop, this header is the primary navigation.

  IMPORTANT
  - No inline CSS.
  - Keep markup stable - iterate via /src/css/styles.css.
  - This file must NEVER contain page front matter (--- blocks).
============================================================================= #}

<header class="topbar">
  <div class="topbar-inner">
    {# ------------------------------------------------------------------------
      Brand / Home link
      - Keep short and tappable.
      - In a restaurant scenario, user can always tap this to reset to search.
    ------------------------------------------------------------------------ #}
    <a class="brand" href="/">Pasta Shapes</a>

    {# ------------------------------------------------------------------------
      Hamburger toggle (checkbox)
      - The label is the visible "hamburger button".
      - The checkbox controls the menu open/closed state in CSS.
      - No JS required.
    ------------------------------------------------------------------------ #}
    <div class="menu">
      <input
        class="menu-toggle"
        type="checkbox"
        id="site-menu-toggle"
        aria-label="Open menu"
      />

      <label class="menu-button" for="site-menu-toggle">
        <span class="menu-icon" aria-hidden="true"></span>
        <span class="sr-only">Menu</span>
      </label>

      {# ----------------------------------------------------------------------
        Menu panel
        - Keep this minimal. You can add more links later.
        - Search is intentionally "/" because search lives on the homepage.
      ---------------------------------------------------------------------- #}
      <nav class="menu-panel" aria-label="Primary">
        <a href="/" {% if page and page.url == "/" %}aria-current="page"{% endif %}>Search</a>

        <a href="/identify/" {% if page and page.url and page.url.startsWith("/identify") %}aria-current="page"{% endif %}>Identify</a>

        <a href="/about/">About</a>
        <a href="/sources/">Sources</a>
        <a href="/feedback/">Feedback</a>
      </nav>
    </div>
  </div>
</header>
