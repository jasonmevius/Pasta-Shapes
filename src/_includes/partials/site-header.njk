{# =============================================================================
  partials/site-header.njk
  ------------------------------------------------------------------------------
  PURPOSE
  - Global top header used by layout.njk on every page.
  - Mobile-first approach:
      - Mobile ( < 720px ): show ONLY the brand (clean + minimal)
        * Primary navigation is handled by the bottom nav (Search / Identify).
      - Tablet/Desktop ( >= 720px ): show a traditional top navigation row.

  WHY THIS CHANGE
  - Your screenshot shows a "Menu" control plus multiple links wrapping.
  - That header pattern clashes with the new mobile bottom nav and creates
    visual noise on small screens.

  IMPORTANT RULES
  - NO inline CSS. Styling lives in /src/css/styles.css
  - Keep markup stable and predictable for future tweaks.
============================================================================= #}

<header class="topbar" role="banner">
  <div class="topbar-inner">
    {# -------------------------------------------------------------------------
      Brand
      - Always visible.
      - Keep it short for mobile.
    ------------------------------------------------------------------------- #}
    <a class="brand" href="/" aria-label="Pasta Shapes home">
      Pasta Shapes
    </a>

    {# -------------------------------------------------------------------------
      Desktop / Tablet navigation
      - Hidden on mobile via CSS.
      - We keep these as plain links for simplicity and reliability.
      - Bottom nav remains the primary navigation on mobile.
    ------------------------------------------------------------------------- #}
    <nav class="nav nav-desktop" aria-label="Primary navigation">
      <a href="/" {% if page and page.url == "/" %}aria-current="page"{% endif %}>Search</a>
      <a href="/identify/" {% if page and page.url and page.url.startsWith("/identify") %}aria-current="page"{% endif %}>Identify</a>
      <a href="/about/" {% if page and page.url and page.url.startsWith("/about") %}aria-current="page"{% endif %}>About</a>
      <a href="/sources/" {% if page and page.url and page.url.startsWith("/sources") %}aria-current="page"{% endif %}>Sources</a>
      <a href="/feedback/" {% if page and page.url and page.url.startsWith("/feedback") %}aria-current="page"{% endif %}>Feedback</a>
    </nav>
  </div>
</header>
