{# =============================================================================
  partials/site-header.njk
  ------------------------------------------------------------------------------
  Sticky header with:
  - Brand (left)
  - Desktop nav (>= 720px)
  - Mobile hamburger (< 720px) that toggles a drawer

  IMPORTANT:
  - Drawer MUST start with `hidden` to avoid "open by default" on first paint.
  - Button uses aria-expanded + aria-controls for accessibility.
============================================================================= #}

<header class="topbar">
  <div class="topbar-inner">
    <a class="brand" href="/">Pasta Shapes</a>

    {# Desktop nav - visible on >= 720px via CSS #}
    <nav class="nav nav-desktop" aria-label="Primary navigation">
      <a href="/" {% if page and page.url == "/" %}aria-current="page"{% endif %}>Search</a>
      <a href="/identify/" {% if page and page.url and page.url.startsWith("/identify") %}aria-current="page"{% endif %}>Identify</a>
      {# Add more items here later as the site grows #}
    </nav>

    {# Mobile hamburger - visible on < 720px via CSS #}
    <button
      id="nav-toggle"
      class="nav-toggle"
      type="button"
      aria-label="Menu"
      aria-expanded="false"
      aria-controls="nav-drawer"
    >
      <span class="nav-toggle-bars" aria-hidden="true">
        <span></span>
        <span></span>
        <span></span>
      </span>
    </button>
  </div>

  {# Mobile drawer - MUST start hidden to prevent "open by default" #}
  <nav id="nav-drawer" class="nav-drawer" aria-label="Menu" hidden>
    <a href="/" {% if page and page.url == "/" %}aria-current="page"{% endif %}>Search</a>
    <a href="/identify/" {% if page and page.url and page.url.startsWith("/identify") %}aria-current="page"{% endif %}>Identify</a>
  </nav>
</header>
