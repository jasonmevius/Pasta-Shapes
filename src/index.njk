---
title: Pasta Shapes
---
<h1>Pasta Shapes</h1>

{% if pasta and pasta.length %}
  <p>Loaded {{ pasta.length }} pasta shapes from pasta.csv.</p>

  <h2>First 10 entries</h2>
  <ol>
    {% for item in pasta | slice(0, 10) %}
      <li>
        {# Try a few common column names - adjust once we know yours #}
        {{ item.name or item.shape or item.pasta or item.title or item[0] }}
      </li>
    {% endfor %}
  </ol>
{% else %}
  <p><strong>CSV not loaded.</strong> Check that the file is at <code>src/_data/pasta.csv</code>.</p>
{% endif %}
